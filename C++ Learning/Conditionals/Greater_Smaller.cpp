#include <iostream>
using namespace std;

// #Coder Army

int main(){
    int a, b; //Initialize the 'a' and 'b' variable 
    cout << "Enter a value: ";
    cin >> a ;                    //Input 'a' value
    cout << "Enter b value: ";
    cin >> b;                     //Input 'b' value

    // Check which Number is Greater
    if(a > b){
        cout << "A is GREATER.";
    }
    else{
        cout << "B is GREATER.";
    }
}